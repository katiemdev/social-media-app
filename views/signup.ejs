<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link type="text/css" rel="stylesheet" href="css/utilities.css" />
		<link type="text/css" rel="stylesheet" href="css/style.css" />
		<link type="text/css" rel="stylesheet" href="css/loginreg.css" />
		<title>Document</title>
	</head>
	<body>
		<main class="container flex">
			<nav class="nav flex">
				<h1><a href="/">Instagram Clone</a></h1>
				<ul class="flex">
					<li><a class="btn login" href="/login">Login</a></li>
					<li><a class="btn register" href="/signup">Register</a></li>
				</ul>
			</nav>
			<div class="form-container flex">
				<!-- ***** error ******-->
				<% if (locals.messages.errors) { %> <% messages.errors.forEach( el =>
				{ %>
				<div class="alert alert-danger"><%= el.msg %></div>
				<% }) %> <% } %> <% if (locals.messages.info) { %> <%
					messages.info.forEach( el => { %>
					<div class="alert alert-info"><%= el.msg %></div>
				<% }) %> <% } %>

				<!-- ***** Sign up page *****-->
					<h1>Sign Up</h1>
					<form class="form grid card" action="/signup" method="POST">
						<div class="username input flex">
							<label for="userName" class="form-label">User Name</label>
							<input
								type="text"
								class="form-control"
								id="userName"
								name="userName"
							/>
						</div>
						<div class="email input flex">
							<label for="exampleInputEmail1" class="form-label"
								>Email address</label
							>
							<input
								type="email"
								class="form-control"
								id="exampleInputEmail1"
								aria-describedby="emailHelp"
								name="email"
							/>
							<p>We'll never share your email with anyone else.</p>
						</div>
						<div class="password input flex">
							<label for="password" class="form-label">Password</label>
							<input
								type="password"
								class="form-control"
								id="password"
								name="password"
							/>
						</div>
						<div class="confirm-password input flex"">
							<label for="confirmPassword" class="form-label"
								>Confirm Password</label
							>
							<input
								type="password"
								class="form-control"
								id="confirmPassword"
								name="confirmPassword"
							/>
						</div>
						<button type="submit" class="btn">Submit</button>
					</form>
				<a href="/" class="btn">Return to Main Page</a>
			</div>
		</main>
	</body>
</html>
